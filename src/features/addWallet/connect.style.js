import styled from 'styled-components';

export const MetaConnectStyle = styled.div`
    width: fit-content;
`;

export const AccountLine = styled.div`
    color: var(--orange);
    max-width: 145px;
    overflow: hidden;
    text-overflow: ellipsis;

    font-weight: 700;
    font-size: 18px;
`;