import styled from 'styled-components';

export const BlockTitleSlyle = styled.h3`
    width: fit-content;
    height: 43px;
    color: var(--orange);
    flex-shrink: 0;

    font-weight: 700;
    font-size: 36px;
`;